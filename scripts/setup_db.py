import sqlite3\n\ndef initialize_database():\n    # Connect to the database (or create it if it doesn't exist)\n    connection = sqlite3.connect('incident.db')\n    cursor = connection.cursor()\n\n    # Create a table for incidents\n    cursor.execute('''CREATE TABLE IF NOT EXISTS incidents (\n        id INTEGER PRIMARY KEY AUTOINCREMENT,\n        title TEXT NOT NULL,\n        description TEXT NOT NULL,\n        date TEXT NOT NULL\n    )''')\n\n    # Commit changes and close the connection\n    connection.commit()\n    connection.close()\n\ndef seed_sample_data():\n    # Connect to the database\n    connection = sqlite3.connect('incident.db')\n    cursor = connection.cursor()\n\n    # Sample incidents data\n    sample_data = [\n        ('Unauthorized Access', 'An unauthorized user tried to access the system.', '2026-03-03 07:50:00'),\n        ('Data Breach', 'Confidential data was leaked to the internet.', '2026-03-03 07:51:00'),\n        ('Denial of Service Attack', 'The service was overwhelmed by traffic.', '2026-03-03 07:52:00')\n    ]\n\n    # Insert sample data into the incidents table\n    cursor.executemany('''INSERT INTO incidents (title, description, date) VALUES (?, ?, ?)''', sample_data)\n\n    # Commit changes and close the connection\n    connection.commit()\n    connection.close()\n\nif __name__ == '__main__':\n    initialize_database()\n    seed_sample_data()